# App-control capabilities

| Capability | Contract |
| --- | --- |
| `health.check` | Return registered-paper, RAW hash, extraction and Intelligence health, restoring missing editable reading copies from immutable extraction data. |

Do not emulate any missing capability with shell commands or direct file access.
